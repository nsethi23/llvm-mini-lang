# Benchmark results

Generated by `bench/run.py` -- do not hand-edit; run `./bench/run.sh` to regenerate.

- Generated: 2026-08-23 22:11 EDT
- Machine: macOS-26.0.1-arm64-arm-64bit-Mach-O, arm
- Python: 3.14.5
- LLVM: 18.1.8
- Promotion threshold: **1000** calls (see PRD.md M8 -- this is a tuning knob, not a fixed constant; too low and promotion overhead dominates, too high and a function never gets compiled)
- Trials: 1 for cold-interpreted (slow, low variance), 5 for tiered and Python (fast; median reported)

| Benchmark | cold-interpreted | tiered (this project) | Python 3 | tiered vs. cold | tiered vs. Python |
|---|---|---|---|---|---|
| fib(30) recursive | 24.77s | 0.0215s | 0.0756s | 1150.3x faster | 3.5x faster |
| sum_loop (10,000 x 1,000 = 10M iterations) | 1.1693s | 0.1466s | 0.2085s | 8.0x faster | 1.4x faster |

## Notes

- **fib(30) recursive**: naive recursive Fibonacci, n=30 (~2.7M calls). Output: `832040`. cold-interpreted is 327.5x slower than Python -- the tree-walking interpreter alone is not competitive; the JIT tier is what makes this project's numbers interesting.
- **sum_loop (10,000 x 1,000 = 10M iterations)**: 10,000 calls to a function looping 1,000 iterations each. Output: `4995000000`. cold-interpreted is 5.6x slower than Python -- the tree-walking interpreter alone is not competitive; the JIT tier is what makes this project's numbers interesting.

Each program's output is cross-checked identical across cold-interpreted, tiered, and Python before its time is recorded (see this script's run_benchmark()).
